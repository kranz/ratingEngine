_schema-version: '2.0'
ID: ratingEngine
version: 0.0.1

modules:
  - name: web
    type: html5
    path: web
    requires:
      - name: ratingEngine-uaa
resources:
  - name: ratingEngine-uaa
    type: com.sap.xs.uaa
    parameters:
      config_path: ./xs-security.json